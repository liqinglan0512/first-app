# Computational Mechanics Solver 主执行计划

## 0. 执行基线

| 项目 | 当前值 |
|---|---|
| 建立日期 | 2026-07-12（Asia/Shanghai） |
| 起始分支 | `work/v1.3.2-manual-homepage` |
| 起始提交 | `68c80537f6e849703b5949fc23b76a1c6f13de7e` |
| 当前工作分支 | `feature/v1.4-auth-commercial-ui` |
| 工作区基线 | 创建分支前干净 |
| 既有发布标签 | 保留 `v1.3.2-beta.1`、`v1.3.2-beta.2`，不得移动或覆盖 |
| 部署边界 | 只允许本地修改、提交和最终 GitHub 分支推送；不得自动部署阿里云 |

## 1. 串行门禁

大目标必须严格按以下顺序执行：

1. **大目标一：v1.3.2 公开说明书重构**
2. **大目标二：v1.4.0-beta.1 账户与视觉系统**
3. **大目标三：v1.4.0-beta.1 商业权益与内部测试体系**
4. **大目标四：v1.5.0-alpha.1 动力学物理核心扩展**
5. **大目标五：集成验证、GitHub 分支与发布交接**

只有当前目标的全部验收项有直接证据后，才允许启动下一目标。不得用测试意图、界面占位或未验证实现替代验收证据。

---

## 2. 大目标一：重构 v1.3.2 公开用户与技术说明书

**状态：完成**
**分支：** `docs/v1.3.2-manual-rewrite`
**计划提交：** `docs: rewrite public v1.3.2 user and technical manual`

### 2.1 输入证据

| 输入 | 状态 | 用途与边界 |
|---|---|---|
| 用户修改版 DOCX | 已定位，当前由 Word 打开且含未保存修改 | 内容优先级最高；必须保存或创建稳定副本后再编辑 |
| 旧版 PDF | 已存在 | 只用于发现排版问题，不覆盖用户 DOCX |
| `web/welcome-bg.jpg` | 已确认 | 整页 A4 封面背景与网站视觉基线 |
| 主页截图 `2026-07-12 011828.png` | 已确认 | 验证版本、公告入口与 Li 上标缺陷 |
| 动力学案例 2 截图与结果文本 | 已收到 | 可作为真实界面与结果证据；无工程文件时标记“无法完全复算” |
| 静力学案例 1 | 已找到三角形平面刚架截图，B 级 | 只可核查界面；无工程文件，无法复算 |
| 静力学案例 2 | 用户于 2026-07-12 明确取消 | 不再属于公开说明书范围 |
| 动力学案例 1 | 已找到均匀磁场轨迹截图，B 级 | 用户确认账号为虚拟信息并授权原图公开 |

### 2.2 小目标与状态

- [x] 1.1 创建用户 DOCX 的稳定只读工作副本并记录来源哈希。
- [x] 1.2 提取并对比用户 DOCX 与旧 PDF 的章节、文字、图表、公式和用户改动。
- [x] 1.3 设计整页星空封面，只保留产品名、版本说明、`Leo Li` 上标 `+`、日期。
- [x] 1.4 建立 A4、页边距、字体、标题、正文、图题、表题、公式、页眉页脚和多级编号样式。
- [x] 1.5 从“第一章”开始重构真实多级标题与可更新目录。
- [x] 1.6 把公开说明书改写为本科生、教师和普通互联网用户可执行的操作手册。
- [x] 1.7 从公开说明书移除本机路径、Git、API、源码包、内部目录、测试文件和发布流程。
- [x] 1.8 把被移除但仍有内部价值的技术与发布证据迁移到开发附录。
- [x] 1.9 建立三个用户案例证据表，区分可复算与仅截图证据。
- [x] 1.10 每个可写入案例覆盖目标、场景、条件、单位、步骤、参数、截图、设置、软件结果、理论结果、误差、解释、错误和限制。
- [x] 1.11 生成可编辑 DOCX 与可搜索 PDF，确保两者内容一致。
- [x] 1.12 更新目录、图表与公式编号，并验证交叉引用。
- [x] 1.13 将 DOCX 全页渲染为 PNG，逐页检查后迭代修复。
- [x] 1.14 执行字体、页面边界、空白页、图片比例、表格宽度、隐私和敏感文本审计。
- [x] 1.15 运行全部既有回归测试并完成 Git 门禁。
- [x] 1.16 创建大目标一独立提交。

### 2.3 计划修改文件

- `CODEX_MASTER_PLAN.md`
- `docs/manual/v1.3.2/Computational-Mechanics-Solver-v1.3.2-用户与技术说明书.docx`
- `docs/manual/v1.3.2/manual-source.md` 或等价结构化源稿
- `docs/manual/v1.3.2/README.md`
- `docs/internal/Computational-Mechanics-Solver-v1.3.2-开发与发布附录.md`
- `web/downloads/computational-mechanics-solver-v1.3.2-manual.pdf`
- `tools/` 下可重复生成、检查 DOCX/PDF 的脚本
- `tests/` 下说明书结构、内容、安全与一致性测试

### 2.4 风险

- 用户 DOCX 当前在 Word 中打开且含未保存修改，直接读取可能得到旧内容或失败。
- 三个用户案例均只有 B 级截图或结果证据，必须持续标记“无法完全复算”；不得补造理论误差。
- Word、LibreOffice 和 PDF 渲染器的目录、分页、字体与公式行为可能不同。
- 星空整页背景、浮动图形和公式对象容易在不同渲染器中错位。
- 公开手册必须删除开发证据，但不能因此把未实现功能写成已实现。

### 2.5 验收证据

- [x] DOCX 可编辑，PDF 可搜索，文本无乱码。
- [x] 封面整页背景、无白边，`+` 为真正上标且不换行。
- [x] 目录从第一章开始且页码正确。
- [x] 标题、正文、图、表、公式、页眉页脚符合指定样式。
- [x] 三个用户案例的证据等级和可复算状态真实明确。
- [x] 公开手册不含本机路径、Git、API、源码包或内部发布信息。
- [x] DOCX/PDF 内容一致，全页 PNG 逐页视觉检查通过。
- [x] 既有 Python 和 JavaScript 回归测试通过。
- [x] `git diff --check` 通过且提交范围只含大目标一。

**测试证据：** Python 48/48、JavaScript 83/83、文档专项 4/4 通过；Python/JavaScript 语法检查通过。
**提交：** `docs: rewrite public v1.3.2 user and technical manual`

---

## 3. 大目标二：v1.4.0-beta.1 真实云账户与视觉系统

**状态：完成**
**计划分支：** `feature/v1.4-auth-commercial-ui`

### 3.1 小目标

- [x] 2.1 编写 `docs/adr/0001-auth-and-cloud-storage.md`。
- [x] 2.2 将全部品牌文字统一为 `Leo Li` + 真上标 `+` + `Studio出品`。
- [x] 2.3 修复公告原子核及所有页面的 Li 上标。
- [x] 2.4 设计 SQLite 开发、PostgreSQL 生产兼容的数据访问层和可追踪迁移。
- [x] 2.5 建立 `users`、`sessions`、`roles`、`entitlements`、`subscription_plans`、`user_entitlements`、`login_attempts`。
- [x] 2.6 使用 Argon2id（依赖受限时 bcrypt）存储密码哈希。
- [x] 2.7 实现服务器注册、登录、会话查询、退出和撤销。
- [x] 2.8 实现 HttpOnly、SameSite、过期、生产 Secure Cookie 与 CSRF 防护。
- [x] 2.9 实现登录限速、统一错误、防用户名枚举和参数化数据库操作。
- [x] 2.10 实现头像 MIME、扩展名、大小、安全文件名与路径穿越防护。
- [x] 2.11 提供旧 localStorage 用户迁移提示，不上传旧密码。
- [x] 2.12 将静力学、动力学改造成分层纯黑主题并保持画布可读性。
- [x] 2.13 美化两处说明书卡片并覆盖 hover、focus、减少动画和移动端。
- [x] 2.14 回归静力学与动力学计算功能。

### 3.2 风险与计划证据

- 认证与现有本地账户状态迁移可能产生双重身份来源。
- PostgreSQL 与 SQLite 方言差异必须通过迁移测试覆盖。
- Cookie 安全属性在本地 HTTP 与生产 HTTPS 下配置不同。
- 深色主题不能通过全局黑色覆盖实现，必须使用设计令牌和视觉回归。

**计划测试：** 注册、哈希、登录、退出、Session 撤销、迁移、限速、CSRF、头像安全、旧计算回归、响应式与无障碍。
**计划提交：** `feat: add cloud accounts and refine dark visual system`
**提交号：** `c7c0e49`（`feat: add cloud accounts and refine dark visual system`）

**验收证据：** Python 64/64、JavaScript 118 项断言和全部语法检查通过；wheel 构建成功且包含 `0001_auth_schema.sql`。真实浏览器已验证注册、刷新恢复会话、昵称持久化、退出、重新登录，以及 1692×960、1366×768、390×844 三种视口；浏览器控制台错误为 0。敏感值扫描只命中允许的环境变量名和 `.env.example` 占位值。

---

## 4. 大目标三：Free、Plus、Pro 与 Internal Tester 权益

**状态：完成**
**计划分支：** `feature/v1.4-auth-commercial-ui`

### 4.1 小目标

- [x] 3.1 编写 `docs/adr/0002-entitlements-and-plans.md`。
- [x] 3.2 建立 Free、Plus、Pro、Internal Tester、Admin 的服务端角色与权益模型。
- [x] 3.3 明确 Internal Tester 不拥有管理员权限。
- [x] 3.4 实现“获得更多权益”按钮与权益面板。
- [x] 3.5 由服务端限制正式报告、高级导出等权益。
- [x] 3.6 PINNs 始终标记“开发中”，只允许等待名单或进度入口。
- [x] 3.7 内部通道只从 `CMS_INTERNAL_INVITE_CODE` 读取配置；仓库、前端、日志和响应中不得出现真实值。
- [x] 3.8 `.env` 保持忽略，`.env.example` 只使用占位值。
- [x] 3.9 实现内测码限速、到期、使用时间记录和管理员撤销。
- [x] 3.10 实现 Internal Tester 彩虹头像光圈及唯一主身份标签。
- [x] 3.11 标签优先级为 Internal Tester > Pro > Plus > Free。
- [x] 3.12 拒绝伪造前端角色和无权限报告请求。

### 4.2 风险与计划证据

- 权益不能只通过隐藏按钮实现，所有受限接口必须服务端检查。
- 内测码、密码、Cookie、哈希和令牌不得进入日志或仓库。
- 商业文案不能把 PINNs 等开发中能力宣传为可用付费功能。

**验收证据：** Python 77/77；全部 JavaScript 测试与语法检查通过。HTTP 回归覆盖正式报告与高级导出拒绝前端伪造、管理员撤销 Internal Tester。真实浏览器已验证 Free、Plus/Pro 只展示不购买、PINN 等待名单不解锁求解器、Internal Tester 不具备 Admin 权限、刷新持久化、彩虹头像光圈和唯一标签；1692×960、1366×768、390×844 三种视口通过，控制台错误为 0。三视口截图与机器可读记录保存在 `release/v1.4.0-beta.1/browser-qa.json`；120 个 Git 跟踪 blob 的凭据模式扫描通过。
**计划提交：** `feat: add plus pro and internal tester entitlements`
**提交号：** `ce7e3d7`（`feat: add plus pro and internal tester entitlements`）

---

## 5. 大目标四：v1.5.0-alpha.1 动力学物理核心扩展

**状态：已完成（2026-07-12）**
**计划分支：** `feature/v1.5-dynamics-physics`

### 5.1 架构决策与模块

- [x] 编写 `docs/adr/0003-collision-and-contact-model.md`。
- [x] 编写 `docs/adr/0004-constraint-track-model.md`。
- [x] 编写 `docs/adr/0005-variable-field-expression.md`。
- [x] 新建纯数值接触、碰撞、约束、刚体、变化场与安全表达式模块。
- [x] 保持 DOM、渲染、状态与物理计算解耦，不继续集中到 `web/app.js`。

### 5.2 碰撞、摩擦与耗散

- [x] 可靠支持圆/圆与带碰撞半径的质点；拒绝未实现几何。
- [x] 实现 `0 <= e <= 1`、静/动摩擦和非负阻尼参数验证。
- [x] 实现检测、法向、相对速度、法/切向冲量、穿透修正和多体迭代。
- [x] 采用自适应子步降低高速穿透，并把外部加速度纳入行程估计。
- [x] 验证动量守恒、弹性动能守恒及非弹性/摩擦耗散。

### 5.3 刚体状态与作用点

- [x] 扩展状态到平动与转动，真实求解角度、角速度和力矩。
- [x] 默认力/冲量作用于形心，自定义作用点保存局部偏移。
- [x] 偏心力进入 `I alpha = sum(tau)`，偏心冲量同时改变线速度与角速度。
- [x] 工程保存、打开和迁移保持作用点。

### 5.4 轨道、斜坡与滚动

- [x] 支持直线、倾斜直线、折线、圆弧和平滑曲线轨道。
- [x] 支持轨道摩擦、恢复系数、离轨和端点处理。
- [x] 区分质点滑动与圆盘无滑动滚动。
- [x] 真正计算转动惯量、`v = R omega`、摩擦与滑动转滚动。
- [x] 输出到达时间、坡底状态、能量、滑动和离轨状态。

### 5.5 变化场与安全表达式

- [x] 重力、电场、磁场支持常量、时间、空间及时间-空间表达式。
- [x] 构建受控 AST；禁止 `eval`、`new Function` 和 Python `eval`。
- [x] 只允许规定变量、运算、函数和常量，并限制长度、深度和计算量。
- [x] RK4 的 k1-k4 使用各自中间 `t, x, y` 计算场，并在每个阶段重新检查有限场范围。
- [x] 检查重力、电场和磁场单位。

### 5.6 风险与计划证据

- 同步世界积分会改变现有“对象独立积分”架构与工程 Schema。
- 碰撞、约束和刚体的能量行为需要按模型分别定义，不能事后修正结果。
- 轨道接触需要稳定处理法向力、离轨、端点和时间步收敛。
- 表达式解析是安全边界，必须有拒绝代码执行和资源耗尽测试。

**验收测试：** 20 组 JavaScript 测试全部通过，Python 77/77，核心 v1.5 数值模块覆盖率 95.12%（行/语句）、95.80%（函数）。覆盖一维/斜向/三体碰撞、净接触耗散、摩擦、强外力高速防穿透、偏心力/冲量、斜面、圆弧、滚动、离轨、端点 `release/open`、轨道外力、有限范围变化场、表达式白名单、RK4 中间场、宿主安全上限、收敛和旧动力学回归。

**浏览器证据：** 真实浏览器完成 v1.5 版本、工作台、新刚体/接触与地面控件、圆形刚体放置、求解、角速度、角加速度、力矩和质心惯量输出验证；宽屏、桌面、手机三视口控制台错误均为 0，证据位于 `release/v1.5.0-alpha.1/browser-qa.json`。

**范围说明：** v1.5.0-alpha.1 的轨道对象仍作为独立约束子系统求解；暂不与自由/接触对象碰撞，离轨后也不接续剩余时长的自由飞行。该边界已写入 ADR 0004，并将在物理验证与发布说明中继续披露。

**提交组：** `72e75c6`（碰撞与接触）、`19722cd`（轨道与滚动）、`b3b7134`（安全变化场）及本次世界层/UI/Schema 集成提交。

---

## 6. 大目标五：集成、验证与 GitHub 交付

**状态：等待大目标四门禁**
**计划集成分支：** `release/v1.5.0-alpha.1`

### 6.1 小目标

- [ ] 5.1 按顺序合并说明书、账户权益和动力学分支，不直接合并 `main`。
- [ ] 5.2 运行全部 Python、JavaScript、文档、安全和物理测试。
- [ ] 5.3 扫描凭据、密码、Cookie、私钥、Token、生产数据库配置、`.env` 和真实内测码。
- [ ] 5.4 生成 `SECURITY_REVIEW.md`。
- [ ] 5.5 生成 `PHYSICS_VALIDATION.md`，记录方程、理论值、数值值、误差、步长、收敛、守恒/耗散和范围。
- [ ] 5.6 生成 `RELEASE_NOTES_v1.5.0-alpha.1.md`。
- [ ] 5.7 生成 `DEPLOYMENT_HANDOFF.md`，只提供人工部署和回滚说明。
- [ ] 5.8 检查 Git 历史、Diff、状态和旧标签未变。
- [ ] 5.9 推送四个指定分支，优先创建 Pull Request，不默认合并 `main` 或创建标签。
- [ ] 5.10 明确记录阿里云未更新、公网容器未修改。

### 6.2 风险与计划证据

- 多分支合并可能产生 Schema、认证与前端冲突，必须逐分支验证。
- 最终安全扫描必须针对 Git 跟踪内容和提交历史新增内容，而非只扫描工作区。
- GitHub 网络或授权失败只能通过现有 SSH/Git Credential Manager 处理，不得把凭据写入 URL。

**计划测试：** 附件要求的 20 类新增测试、全部旧测试、文档渲染、浏览器多视口、敏感值扫描、Git 引用核验。
**提交号与 PR：待生成。**

---

## 7. 执行日志

### 2026-07-12 - 建立计划

- 当前状态：仅启动大目标一。

### 2026-07-12 - 大目标一阶段检查点

- 用户未保存 DOCX 已通过 Word 格式化内容副本冻结，原文档保持打开且未被覆盖；副本 SHA256 为 `af1b4567c9909917ebed4ea981027a59f332e19fa6ccd7b7f9ea483a8ff70c77`。
- `welcome-bg.jpg` 已作为无白边 A4 封面，`Leo Li` 的 `+` 使用单独上移的小字号绘制在右上角。
- 公开文本扫描未发现本机路径、Git、API、SHA256、源码包、内部发布路径或 Unicode 替换字符。
- 内部开发与发布证据已迁移到 `docs/internal/Computational-Mechanics-Solver-v1.3.2-开发与发布附录.md`。
- 已核对：工作区干净；从 `68c8053` 创建 `docs/v1.3.2-manual-rewrite`。
- 已确认：星空背景、主页 Li 上标缺陷截图、动力学案例 2 截图与结果文本。
- 当前风险：用户 DOCX 正由 Word 打开且存在未保存修改，需先取得稳定副本；其余三个用户案例证据尚待清点或补充。
- 下一步：取得稳定 DOCX 副本，渲染并对比用户版与旧版 PDF，建立大目标一内容和排版基线。

### 2026-07-12 - 用户调整案例范围

- 用户明确取消第二个静力学用户案例，最终范围为 1 个静力学用户案例和 2 个动力学用户案例。
- 用户确认两张动力学原图中的头像与账号均为虚拟信息，授权直接写入公开 DOCX。
- 两张动力学原图已按原始哈希加入，未裁剪、未重绘；正文使用 Word 图号和交叉引用。
- 最终候选为 15 章、26 页、80 个目录条目、20 个公式对象、10 个表格、3 张正文案例图和 4 个 DOCX 媒体资源。
- 26 页已重新渲染并逐页检查；新增动力学图片比例、分页、图题和结果栏清晰度通过。
- 按最新用户范围，案例与隐私门禁已解除；剩余步骤为最终全量回归、Git 审计和独立提交。

### 2026-07-12 - 大目标二账户与视觉实现

- 从说明书提交 `88027e4` 创建 `feature/v1.4-auth-commercial-ui`，未改写任何 v1.3.2 标签。
- 建立 SQLite/PostgreSQL 数据访问层、可追踪迁移、Argon2id 密码、可撤销会话、CSRF、同源校验、登录限速和安全头像存储。
- 前端身份来源已改为服务器会话；旧 `cms_users`/`cms_current_user` 只按键名检测和删除，不解析或上传旧密码。
- 静力学与动力学默认使用分层纯黑主题；两处说明书卡片统一为彩虹描边黑玻璃样式。
- 浏览器真实流程与三种视口、全量回归、wheel 构建、Diff 检查及敏感值扫描均已通过；大目标二门禁完成。

### 2026-07-12 - 大目标三权益系统冻结

- 服务端建立 Free、Plus、Pro、Internal Tester 与 Admin 权益矩阵；Internal Tester 明确不继承管理员能力。
- 正式报告和高级导出在服务端校验；浏览器自报角色不能提升权限。
- PINN 仅提供等待名单，求解器选项保持禁用并持续标记“开发中”。
- 内部通道只读取 `CMS_INTERNAL_INVITE_CODE`，数据库仅保存 HMAC 指纹；错误尝试限速、到期恢复和管理员撤销均有自动测试。
- 权益面板已将“当前主身份”改为“当前身份”；说明书卡片图标改为六色线框正六面体。
- 浏览器验证覆盖 Free、Internal Tester、刷新持久化、三种视口和彩虹头像光圈，控制台错误为 0。
- Python 77/77、全部 JavaScript 测试和语法检查通过；120 个 Git 跟踪 blob 敏感模式扫描通过。
- 补充高级导出拒绝、管理员撤销 HTTP 回归测试，并提交三视口截图与 `browser-qa.json`；刷新会话、Internal Tester 唯一标签、无 Admin 权限与 PINN 等待名单不提权均可独立复核。
- 功能提交：`ce7e3d7`。

### 2026-07-12 - 大目标四动力学物理核心完成

- 建立同步二维刚体世界、圆/圆与圆/地面接触、法/切向顺序冲量、摩擦、恢复、穿透修正和含外部加速度的自适应子步。
- 工程 Schema 升级到 `cms-dynamics-project@2`，保留 v1 迁移，保存角状态、接触材料、作用点、轨道、约束、地面与变化场。
- 建立直线、斜坡、折线、圆弧、Bezier 轨道以及质点滑动、实心圆盘滚动、离轨和端点事件。
- 变化场使用受控 AST，不执行用户代码；RK4 四阶段按各自时间、位置、速度与有限场范围求值。
- 20 组 JavaScript、Python 77/77、语法检查和 95.12% 核心模块行覆盖率通过；三视口真实浏览器控制台错误为 0。
- 明确 alpha 范围：轨道对象与自由接触世界尚未耦合，离轨后不接续自由飞行；不得外推为已支持功能。
